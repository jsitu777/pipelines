apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Values.labels.app }}
  name: kubeflow-pipelines-profile-controller
  namespace: {{ .Values.namespace}}
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: {{ .Values.labels.app }}
